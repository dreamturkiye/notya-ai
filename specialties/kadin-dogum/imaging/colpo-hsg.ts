/** Kolposkopi and HSG archive — coreImageId only, wrap core görüntüleme. */
import type { ColpoImage, HsgImage } from '../schema'

export function archiveColpo(img: ColpoImage): ColpoImage {
  return img
}

export function archiveHsg(img: HsgImage): HsgImage {
  return img
}
